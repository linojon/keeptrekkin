.row
  .col-sm-8.col-sm-offset-2
    %h1 Activities Log
    - @activities.each do |activity|
      %p
        #{activity.trackable.updated_at}: 
        = link_to activity.owner.name, activity.owner if activity.owner
        = render_activity activity



