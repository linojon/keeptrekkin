%nav.navbar.navbar-inverse.navbar-fixed-top{ role: :navigation }
  .container-fluid
    / mobile 
    .navbar-header
      %button.navbar-toggle{ type: :button, data: { toggle: :collapse, target: '#navbar-collapse' } }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{ href: '/' }= app_title
  
    #navbar-collapse.collapse.navbar-collapse
      %ul.nav.navbar-nav
        %li.dropdown
          %a.dropdown-toggle{ href:'#', 'data-toggle' => 'dropdown'} 
            Destinations
            %span.caret
          %ul.dropdown-menu{ role: 'menu'} 
            %li= link_to 'New Hampshire 4000 Footers', mountains_path
        %li= link_to 'Trips', trips_path

      %ul.nav.navbar-nav.navbar-right
        - if current_hiker
          %li= link_to current_hiker.name, profile_path
          %li= link_to 'Sign out', signout_path, id: 'sign_out'
        - else
          %li= link_to 'Sign in via Facebook', '/auth/facebook', id: 'sign_in'

      / %ul.nav.navbar-nav.navbar-right
      /   - if current_user
      /     %li= link_to 'Account', account_path
      /     %li= link_to 'Profile', edit_user_registration_path
      /     %li= link_to 'Sign out', destroy_user_session_path, method: :delete
      /   - else
      /     %li= link_to 'Sign in', new_user_session_path
