!!!
%html
  %head
    %meta{ content: 'text/html; charset=UTF-8', 'http-equiv' => 'Content-Type' }
  %body
    %h1 
      Hi #{@hiker.name}! 
    %p 
      You've been added to a hiking trip on KeepTrekkin.com. Check out the trip report, edit the journal, and add photos. 
    - if @trip.mountains.present?
      %h3
        Trip to #{ @trip.mountains.map(&:name).join(', ')}
    %p
      No sign-up is needed. Just visit #{@url} and connect via your Facebook account. 
    %p
      %small For authentication only, we will never share your info or post without your direction.
    -if @trip.title_image.present?
      %p.centered
        = cl_image_tag @trip.title_image.path, size: '300x200', crop: :fill, class: 'img-rounded'
